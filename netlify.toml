[build]
  base = "crypt"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19.0"
  VITE_FIREBASE_AUTH_DOMAIN = "digilab.medialit.in"

# SPA redirect - send all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
